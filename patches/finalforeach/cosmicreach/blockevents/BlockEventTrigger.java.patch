--- a/finalforeach/cosmicreach/blockevents/BlockEventTrigger.java
+++ b/finalforeach/cosmicreach/blockevents/BlockEventTrigger.java
@@ -8,8 +_,11 @@
 import finalforeach.cosmicreach.world.Zone;
 import java.util.Map;
 
+import static com.github.puzzle.paradox.game.event.BlockEventEvents.ON_BLOCK_EVENT_ACT;
+
 public class BlockEventTrigger implements Serializable {
 	private IBlockAction action;
+	public boolean shouldAct;
 
 	@Override
 	public void write(Json json) {
@@ -32,8 +_,12 @@
 	}
 
 	public void act(BlockState srcBlockState, Zone zone, Map<String, Object> args) {
+
+		ON_BLOCK_EVENT_ACT.invoker().onBlockEvenAct(this,srcBlockState,zone,args);
+
 		try {
-			this.action.act(srcBlockState, this, zone, args);
+			if(shouldAct)
+				this.action.act(srcBlockState, this, zone, args);
 		} catch (Exception exception) {
 			if (this.action != null) {
 				throw new RuntimeException("Error while triggering block event action " + this.action.getActionId() + " for block: " + srcBlockState, exception);
