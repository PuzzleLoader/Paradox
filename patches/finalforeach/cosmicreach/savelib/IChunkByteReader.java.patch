--- a/finalforeach/cosmicreach/savelib/IChunkByteReader.java
+++ b/finalforeach/cosmicreach/savelib/IChunkByteReader.java
@@ -2,20 +_,19 @@
 
 import java.io.IOException;
 
-public interface IChunkByteReader {
+public interface IChunkByteReader
+{
 	int readInt() throws IOException;
-
 	byte readByte() throws IOException;
-
 	String readString() throws IOException;
-
 	short readShort() throws IOException;
 
-	default void readFully(byte[] bytes) throws IOException {
-		int i = bytes.length;
-
-		for (int j = 0; j < i; j++) {
-			bytes[j] = this.readByte();
+	default void readFully(byte[] bytes) throws IOException 
+	{
+		int len = bytes.length;
+		for(int i = 0; i < len; i++) 
+		{
+			bytes[i] = readByte();
 		}
 	}
 }
